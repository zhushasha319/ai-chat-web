@template vue3-component
@type modal
@basecomponent BaseDialog
@description 创建一个通用的模态对话框组件，支持标题、内容插槽、确认和关闭事件。
@needs props, emits, v-model, lifecycle, expose
@note:
- 使用 Vue3 组合式 API
- 提供默认插槽支持
- 支持 `visible` 的双向绑定
- 提供 `open` 和 `close` 方法
- 提供 `confirm` 事件
<template>
  
</template>

<script>
export default {

}

</script>

<style scoped>

</style>

